name: Jenkins Backup

on:
  push:
    branches: [ "develop" ]
  workflow_dispatch:

jobs:
  call :
    runs-on: 
    steps:
      - name: Call Jenkins
        run: |
          curl -kv telnet://192.168.136.73:7010